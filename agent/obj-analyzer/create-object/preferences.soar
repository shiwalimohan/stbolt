# Preferences for create-object operators
# copy-att-val > lookup-att > lookup-val > comprehend-att > complete-create-object

# Rank 1: create-object
sp {create-object*elaborate*pref-rank*copy-att-val
   (state <s> ^name create-object
              ^operator <o> +)
   (<o> ^name copy-att-val)
-->
   (<o> ^pref-rank 1)
}

# Rank 2: lookup-att
sp {create-object*elaborate*pref-rank*lookup-att
   (state <s> ^name create-object
              ^operator <o> +)
   (<o> ^name lookup-att)
-->
   (<o> ^pref-rank 2)
}

# Rank 3: lookup-val
sp {create-object*elaborate*pref-rank*lookup-val
   (state <s> ^name create-object
              ^operator <o> +)
   (<o> ^name lookup-val)
-->
   (<o> ^pref-rank 3)
}

# Rank 4: comprehend-att
sp {create-object*elaborate*pref-rank*comprehend-att
   (state <s> ^name create-object
              ^operator <o> +)
   (<o> ^name comprehend-att)
-->
   (<o> ^pref-rank 4)
}

# Rank 5: complete-create-object
sp {create-object*elaborate*pref-rank*complete-create-object
   (state <s> ^name create-object
              ^operator <o> +)
   (<o> ^name complete-create-object)
-->
   (<o> ^pref-rank 5)
}