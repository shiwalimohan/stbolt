# Learn the attribute
sp {process-message*propose*learn-att
   (state <s> ^name process-message
              ^learned-att <la>)
   (<la> -^learned true)
-->
   (<s> ^operator <o> +)
   (<o> ^name learn-att
        ^learned-att <la>)
}