sp {prefer*all*over*complete-att-rel-info
   (state <s> ^name att-rel-info
              ^operator <o> +)
   (<o> ^name complete-att-rel-info)
-->
   (<s> ^operator <o> <)
}

sp {prefer*init-att-rel-info*over*all
   (state <s> ^name att-rel-info
              ^operator <o> +)
   (<o> ^name init-att-rel-info)
-->
   (<s> ^operator <o> >)
}