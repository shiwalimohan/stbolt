# Operator applications regarding the output link

# Copies actions from an operator to the output-link
sp {apply*operator*create-action-command
   (state <s> ^operator <o>
              ^io.output-link <ol>)
   (<o> ^actions <act>)
   (<act> ^<att> <value>)
-->
   (<ol> ^<att> <value>)}

# removes structures from the output link marked complete or error
sp {apply*operator*remove-command
   (state <s> ^superstate nil
              ^operator <o>
              ^io.output-link <ol>)
   (<ol> ^<att> <value>)
   (<value> ^status << complete error >>)
-->
   (<ol> ^<att> <value> -)}