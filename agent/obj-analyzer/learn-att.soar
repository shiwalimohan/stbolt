# Learn the attribute
sp {top-statee*propose*learn-att
   (state <s> ^name obj-analyzer
              ^learned-att <la>)
   (<la> -^learned true)
-->
   (<s> ^operator <o> +)
   (<o> ^name learn-att
        ^learned-att <la>)
}