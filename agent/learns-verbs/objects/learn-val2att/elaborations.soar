# Adds information about the learned attribute to the state
sp {learn-val2att*elaborate*attribute*info
   (state <s> ^name learn-val2att
              ^superstate.operator <o>)
   (<o> ^attribute <external-att>
        ^value <external-val>)
-->
   (<s> ^external-att <external-att>
        ^external-val <external-val>)
}