sp {find-object*propose*init-find-object
   (state <s> ^name find-object
             -^description
              ^superstate.operator.description <desc>)
-->
   (<s> ^operator <o> + >)
   (<o> ^name init-find-object)
}

sp {find-object*apply*init-find-object
   (state <s> ^name find-object
              ^operator.name init-find-object
              ^superstate.operator.description <desc>)
-->
   (<s> ^description <desc>)
}

sp {find-object*apply*init-find-object*copy*objects
   (state <s> ^name find-object
              ^operator.name init-find-object
              ^topstate.object <obj>)
-->
   (<s> ^object <obj>)
}