sp {apply*init-agent*topchef-soar
   (state <s> ^operator <op>)
   (<op> ^name init-agent)
-->
   (<s> ^name topchef-soar)   
}

sp {elaborate*predicates*for*all*states
   (state <s> ^superstate <any>)
-->
   (<s> ^vpredicates <p>)
}