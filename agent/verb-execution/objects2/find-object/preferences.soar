sp {find-object*prefer*lookup-smem*over*object-match
   (state <s> ^name find-object
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name lookup-smem)
   (<o2> ^name << object-match no-match >>)
-->
   (<s> ^operator <o1> > <o2>)
}

sp {find-object*prefer*object-match*over*no-match
   (state <s> ^name find-object
              ^operator <o1> +
              ^operator <o2> +)
   (<o1> ^name object-match)
   (<o2> ^name no-match)
-->
   (<s> ^operator <o1> > <o2>)
}