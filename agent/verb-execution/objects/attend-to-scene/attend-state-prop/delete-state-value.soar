sp {attend-to-scene*propose*delete-state-value
   (state <s> ^name attend-to-scene
              ^object-pair <op>
              ^parameters.create-delete-only false)
   (<op> ^il <il-obj>
         ^ts <ts-obj>)
   (<ts-obj> ^property <ts-prop>)
   (<ts-prop> ^name <prop-name>
              ^value <val>)
   (<val> ^word <word>)
   (<il-obj> ^property <il-prop>)
   (<il-prop> ^type state
              ^name <prop-name>
              -^value <word>)
-->
   (<s> ^operator <o> + =)
   (<o> ^name delete-state-value
        ^category object-processing
        ^level value
        ^property <ts-prop>
        ^value <val>)
}

sp {attend-to-scene*reject*delete-state-value*not-specified
   (state <s> ^name attend-to-scene
              ^parameters <p>
              ^operator <o> +)
   (<o> ^name delete-state-value
        ^property.name <name>)
   (<p> ^use-all-properties false
       -^use-property <name>)
-->
   (<s> ^operator <o> -)
}

sp {attend-to-scene*apply*delete-state-value
   (state <s> ^name attend-to-scene
              ^operator <o>)
   (<o> ^name delete-state-value
        ^property <ts-prop>
        ^value <label>)
-->
   (<ts-prop> ^value <val> -)
}