# elaborations
# copies the value onto the substate

# If there is an internal value on the superstate operator, add it to the query
sp {lookup-val2att*elaborate*internal
   (state <s> ^name lookup-val2att
              ^superstate.operator.value <val>)
-->
   (<s> ^value <val>)
}