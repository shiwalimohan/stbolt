Changes made to abolt:

1. Change sensingRange of all sensables to where they can be sensed anywhere (2 seems to be sufficient)

2. Add information to the robot's sensable to indicate when it is holding an object

	Add the following method to the SimGrabbable interface:
		int getID();
	
	Then make the following change to SimBoltRobot.java line 277:
	String robotInfo = String.format("ROBOT_POS=[%.3f %.3f %.4f],BATTERY=%.1f", pos[0],pos[1],rpy[2], 11.1);
    if(grabbedObject != null){
          robotInfo += ",HOLDING=" + grabbedObject.getID();
    }
    sensList.add(robotInfo);
    
3. Update a SimBoltObject so that its position is reported in object_data_t

	Add the following method to SimBoltObject interface:
		public double[] getPosition();
		
	Then in SimChicken implement the method:
		public double[] getPosition(){
        	return LinAlg.matrixToXYT(pose);
    	}
    	
    Then add the following line to SimBoltRobot.java line 305:
    	obj_data.pos = bo.getPosition();