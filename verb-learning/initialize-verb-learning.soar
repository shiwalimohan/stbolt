## while initializing the agent, create instruction related WMEs.
## maintain instruction state stack on the topstate to determine if an instruction applies to which state


## the agent also maintains - conversation-state

sp {topstate*apply*init*instruction
   (state <s> ^operator <op>)
   (<op> ^name init-agent)
-->
   (<s> ^state-stack <iss>
	^task <t>)
   (<iss> ^current <cs>)
   (<cs> ^name <sname>
	  ^next none
	  ^last-performed-action none)
   (dont-learn <s>)
}
